# hypridle config (currently empty)
# Place idle / lock / suspend listeners here.
# Exemplo:
#   listener {
#       timeout = 300
#       on-timeout = loginctl lock-session
#       on-resume = notify-send "Resumed"
#   }
