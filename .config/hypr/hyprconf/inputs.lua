-- ============================================================================
-- Input devices (keyboard, touchpad, mouse)
-- ============================================================================

hl.config({
  input = {
    kb_layout = "pt",
    numlock_by_default = true,
    follow_mouse = 1,

    touchpad = {
      natural_scroll = true,
    },

    mouse = {
      -- Perfil padrão de Niri
    },
  },
})
